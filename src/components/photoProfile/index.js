import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const PhotoProfile = () => {
  return (
    <View>
      <Text>PhotoProfile</Text>
    </View>
  )
}

export default PhotoProfile

const styles = StyleSheet.create({})